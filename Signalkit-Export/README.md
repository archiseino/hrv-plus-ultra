## 🛠️ Building the App

Just run the `pyinstaller --clean .\Kivy.spec`.

Requirement Python 3.10.18 for Pyinstaller for this case

Using the requirement from the .env

This still leaves a problem when build with python 11 with newer deps. Python doesn't natively built for this
